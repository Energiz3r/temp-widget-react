import { style } from "@vanilla-extract/css";

export const styles = {
  main: style({
    position: "absolute",
  }),
  outer: style({
    position: "absolute",
    width: "100%",
    height: "100%",
    cursor: "pointer",
  }),
};
